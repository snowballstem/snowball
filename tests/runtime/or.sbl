// Test `or`.

externals (stem)
routines (a)
integers (x)

define a as
    try (
        next
        $(x < 2)
    )

define stem as (
    $x = limit
    a or fail='a did not signal t'
    $(cursor == 0) or fail='a did not restore cursor'
)
