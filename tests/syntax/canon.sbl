// Test canonicalisation of commands.
externals (stem)
integers (x y)
define stem as (
  $x = cursor
  setmark y

  try 'a'

  atmark x
  $(cursor == y)

  atlimit
  $(cursor >= limit)

  // <-'' is the same as delete.
  [substring] among (
    'a' (delete)
    'b' (<-'')
  )

  $(x == y)
)
